'use client';

import AuthPanel from '@/components/AuthPanel';

export default function LoginPage() {
  return (
    <main className="page">
      <div className="auth-page">
        <AuthPanel />
      </div>
    </main>
  );
}
